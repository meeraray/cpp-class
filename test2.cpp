#include <iostream>
#include <cmath>
#include <vector>

using namespace std;

int main() {
	const double RD_ONLY = 0.04;
	vector<int> arr();
	// arr.push_back(1);
	// arr.push_back(3);
	// arr.pop_back();
	//selection sort
	// for(int i = 0; i < arr.size(); i++) {
		// cout << arr[i];
	// }
}